import React from 'react';

export const Toaster: React.FC = () => {
  // The toast functionality is handled by the toast manager
  // This component exists for consistency but doesn't render anything
  return null;
};